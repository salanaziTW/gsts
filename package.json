{
  "name": "netlify-blobs-test",
  "version": "1.0.0",
  "description": "Simple Netlify Blobs test project",
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('No build step needed. Static files are in public/.')\""
  },
  "dependencies": {
    "@netlify/blobs": "latest"
  },
  "engines": {
    "node": ">=18"
  }
}
