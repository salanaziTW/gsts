{
  "name": "netlify-simple-task-dashboard",
  "version": "1.0.0",
  "description": "Simple Arabic shared task dashboard using Netlify Functions and Netlify Blobs",
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('No build step needed. Static files are in public/.')\""
  },
  "dependencies": {
    "@netlify/blobs": "latest"
  },
  "engines": {
    "node": ">=18"
  }
}
